# Theia has native deps (@theia/ffmpeg, node-pty) whose old node-gyp scripts
# import distutils, removed in Python 3.12+. Pin a distutils-capable Python
# (macOS CLT ships 3.9 here). Overridable via the PYTHON env var.
python=/usr/bin/python3
fund=false
audit=false
